using System;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using Agent_Aries;
using Wepwawet.Lib;
using AIWolf.Lib;
using Wepwawet.Condition;
using AIWolf.ReproServer;
using System.Collections.Generic;

namespace UnitTestProject
{
    [TestClass]
    public class UnitTest1
    {
        const int loop = 100000;
        
        /*
         * ë¨ìxån
        [TestMethod]
        public void isWolf1()
        {
            Agent agent = Agent.GetAgent(8);
            for(int i = 0; i < loop; i++)
            {
                bool a = pattern.IsExist(agent, Role.WEREWOLF);
            }
        }

        [TestMethod]
        public void isWolf2()
        {
            Agent agent = Agent.GetAgent(8);
            for(int i = 0; i < loop; i++)
            {
                bool a = pattern.WerewolfAgent.Contains(agent);
            }
        }
        */

    }
}
